{
  "name": "@agent-musaj/core",
  "version": "0.1.0",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "tsx --test src/**/*.test.ts",
    "check": "tsc -p tsconfig.json --noEmit"
  }
}
